# with-subcommands-js

JavaScript (ESM) counterpart of [`with-subcommands`](../with-subcommands). Registers
`/math add|subtract` via `registerApplicationCommands` + `registerSubcommand`.

```bash
cp .env.example src/.env
pnpm --filter with-subcommands-js dev
```
