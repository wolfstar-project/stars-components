import { defineConfig } from '@wolfstar/http-framework/config';

// Entry, build output, tsdown, auto imports, development mode, env files, and src/locales are all conventional.
export default defineConfig({});
