import { setup as envRun } from '@wolfstar/env-utilities';
/* oxlint-disable import/first -- side-effect setup modules must run after env load preparation */
import './logger.js';

export function setup() {
	// From dist/lib/setup → .env at the project root
	envRun(new URL('../../../.env', import.meta.url));
}
