/** Helpers for code that runs inside the Nitro app (the Stars entry, or its own imports). */
export { getRouteRules } from './runtime/route-rules.js';
export { useRuntimeConfig, type NitroRuntimeConfig } from './runtime/runtime-config.js';
