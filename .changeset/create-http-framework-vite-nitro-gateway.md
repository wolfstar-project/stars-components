---
'@wolfstar/create-http-framework': minor
---

Add the `vite` and `vite-nitro` build tools to `--build`, and scaffolding for `@wolfstar/plugin-gateway`, `@wolfstar/plugin-cache` (in memory or Redis) and `@wolfstar/plugin-sharder` through the new `--gateway`, `--cache`, `--redis` and `--sharder` options.
