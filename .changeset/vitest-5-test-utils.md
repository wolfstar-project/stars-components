---
'@wolfstar/http-framework-test-utils': major
---

Require `vitest >=5.0.0` as optional peer dependency. The `Assertion` augmentation in `@wolfstar/http-framework-test-utils/vitest` now matches Vitest 5's `Assertion<R, T>` type parameters.
