---
'@wolfstar/create-http-framework': minor
---

Improve CLI with Vite-inspired argument parsing (`--yes`, `--overwrite`, `--interactive`, `--help`), automatic AI agent detection via `@vercel/detect-agent`, optional AI SDK scaffolding (Anthropic/OpenAI), parallel npm version fetching, and directory overwrite handling.
