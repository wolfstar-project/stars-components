---
'@wolfstar/schema': minor
'@wolfstar/http-framework': minor
---

Align configuration entry points with Nuxt: add framework schema and package metadata exports, provide a lightweight schema/config helper, and separate schema input types from config resolution while preserving existing exports.
